#include "board.h"

bool Board::isCheck(int color)
{
    if (color == black_turn)
    {
        for (int i = 0; i < 5; i++)
        {
            for (int j = 0; j < 8; j++)
            {
                
            }
        }
    }
    return true;
}